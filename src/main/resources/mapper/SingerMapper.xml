<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxnan.reper.mapper.SingerMapper">

    <insert id="insert">
        insert into singer(name, sex, pic, birth, location, introduction, create_time, update_time)VALUES
        (#{name},#{sex},#{pic},#{birth},#{location},#{introduction},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update singer
        <set>
            <if test="name!=null and name!=''">name=#{name},</if>
            <if test="sex!=null">sex=#{sex},</if>
            <if test="pic!=null">pic=#{pic},</if>
            <if test="birth!=null">birth=#{birth},</if>
            <if test="location!=null">location=#{location},</if>
            <if test="introduction!=null">introduction=#{introduction},</if>
            <if test="updateTime!=null">update_time=#{updateTime},</if>
        </set>
        where id=#{id}
    </update>
    <select id="criteriaQuery" resultType="com.xxnan.reper.pojo.entity.Singer">
        select * from singer
        <where>
            <if test="name!=null and name!=''">and name like concat('%',#{name},'%')</if>
            <if test="sex!=null and sex!=''">and sex=#{sex}</if>
        </where>
#         order by name;
    </select>
</mapper>